#include <stdio.h>
#include <math.h>

int main () {

    int x = 2;
    
    // x = sqrt(x); //square root(akar)
    // x = pow(x, 3); //power(pangkat)

    printf("%d\n", x);
    printf("hello world\n");
    

    return 0;
}
