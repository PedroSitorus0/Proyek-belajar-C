#include <stdio.h>
#include <math.h>
#include <stdlib.h> // jangan lupaa ditambah jikaa ingin melakukan perhitungan abs

int main () {

    // float x = 3.14;
    float x = 45;
    // x = sqrt(x); //square root(akar)
    // x = pow(x, 3); //power(pangkat)
    // x = round(x); //round(pembulatan ke bilangan terdektat)
    // x = ceil(x); //ceiling(pembulatan ke bilangan tertinggi)
    // x = floor(x);    //floor (pembulatan ke bilangan terendah)
    // x = abs(x); //pembalik dari negatif ke positif (hanya jika negatif)
    // x = log(x); //logaritma
    // x = sin(x);
    // x = cos(x);
    // x = tan(x);
    printf("%f\n", x); //ganti %lf dengan %d/i jika ingin menjumlahkan bilangan yang sudah beberntuk desimal
    printf("hello world\n");
    

    return 0;
}
